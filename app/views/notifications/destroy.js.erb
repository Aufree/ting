$(function(){
    $('#notification-<%= @notification.id %>').addClass('animated lightSpeedOut')
                               .one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', 
        function(){
        $(this).remove();
        }
    );
});