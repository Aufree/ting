$('.ui.form').removeClass('loading')
<% if @user.errors.empty? %>
  Turbolinks.visit('<%= root_path %>');
<% else %>
  $('#signup-errors').html("<%= j render 'shared/error_messages', object: @user %>");
  $('#error_explanation').transition('tada');
<% end %>